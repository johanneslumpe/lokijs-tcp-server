{
  "name": "loki-tcp-server",
  "version": "0.0.1",
  "description": "A tcp server to query a lokijs instance via the network",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Johannes Lumpe",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.1.1",
    "debug": "^2.2.0",
    "lokijs": "^1.3.11"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015-node4": "^2.0.2"
  }
}
